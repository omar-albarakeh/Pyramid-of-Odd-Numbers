/*function poddnumbers(num) {
    for (let i = 0; i < num; i++) {
        let nbs = "";
    for (let j=1; j < 2*i + 2; j +=2) {
        nbs += j + " "; 
    }
    console.log(nbs.trim());
}
}
const x = parseInt(prompt("ur number:"));
poddnumbers(x);*/
